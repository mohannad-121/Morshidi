# Morshidi — Codex Progress Record

## Controlled GitHub handoff -- Draft PR created (2026-09-25)

**Branch/remote:** `chore/p8-recovery-baseline` was pushed to `origin` only; `main` was
not changed, merged, force-pushed, or deployed. The actual implementation commits are
`a5d244767972e9706cbee96f97708b121b79e2a7` (recovery/instructions),
`ef7c27fd9dbfc30a1a73fa796793114bdb9b3c59` (P8 ledger foundation),
`6c26ae8370a99c1794830a2b7fed9b81da39c6c2` (P8 trusted persistence/internal mapper),
and `4b2a07efaf589562d8482f2bc795daa79cf2884e` (P6 outbox/replay integrity).

**Owner review:** Draft PR [#1 — P8 Decision Trace Foundation and P6 Outbox — Review](https://github.com/mohannad-121/Morshidi/pull/1)
targets `main` from `chore/p8-recovery-baseline`. It explicitly records that P8 Slice 2
is **NOT ACCEPTED**, production deployment is **NOT APPROVED**, and owner approval is
required before any merge. No auto-merge was enabled.

## Controlled GitHub handoff audit -- proposed owner review (2026-09-25)

**Scope:** audit and proposed delivery plan only; no staging, commit, push, pull request,
branch switch, reset, clean, production access, migration application, or implementation
change. The candidate handoff is the existing `chore/p8-recovery-baseline` working tree
at `721b317fbb73e6de0cadc8201c6ea1f6849433dc`, targeting `main` only through a future
owner-reviewed Draft PR.

**Audit evidence:** the candidate contains the narrow `.gitignore` rule for the existing
local Python 3.11 virtual environment; engineering/recovery documentation; P8 Slice 2A
ledger/evidence database foundation; Slice 2B repository/service/hardening; P6
transactional outbox plus forward-only replay-integrity correction; and the internal-only
P6 outbox projection/mapper and tests. The three new migrations are additive and later
than the eight tracked baseline migrations; no tracked historical migration is modified.
Review artifacts and virtual environment are excluded, and no source file contains a
credential value. The mapper/local test remains limited: outbox direct access is revoked
even for `service_role`, so the database-backed trusted projection and any processor are
**BLOCKED**, not silently bypassed.

**Actual test evidence:** focused P6/P8 set completed **88 passed, 0 failed, 0 skipped in
24.08s**. Full root command with process-only isolated local Supabase values,
`apps/api/.venv311/Scripts/python.exe -m pytest apps/api/tests -q -rs`, completed
**1412 passed, 0 failed, 0 skipped, 2 warnings in 5983.02s (1:39:43)**. The warnings are
the existing FastAPI/Starlette test-client deprecations. This is local evidence only and
does not make P8 Slice 2 accepted or production approved.

**Proposed review gate:** do not run any Git write action until the owner explicitly says
`APPROVED: COMMIT AND PUSH REVIEW BRANCH`. The proposed Draft PR title is *P8 Decision
Trace Foundation and P6 Outbox — Review*, base `main`, head
`chore/p8-recovery-baseline`, with P8 Slice 2 explicitly NOT ACCEPTED and production
deployment NOT APPROVED. Exact file groupings and commands are prepared in the handoff
report, not executed here.

## Prompt 13B.1 -- trusted P6 outbox projection and deterministic mapper (2026-09-25, checkpoint 1)

**Starting branch/SHA:** `chore/p8-recovery-baseline` /
`721b317fbb73e6de0cadc8201c6ea1f6849433dc`. **Approved local scope:** a typed
internal projection contract and deterministic map for `MOCK_REGISTRATION_SUBMIT` only;
no migration, P8 append, outbox state transition, processor/worker/claim/retry, route,
frontend, academic-state change, reset, hosted access, commit, or production deployment.

**Implemented:** added `apps/api/app/decision_trace_persistence/p6_outbox_mapper.py`.
It accepts only `TrustedP6OutboxProjection` (immutable P6 revision plus outbox typed
record and authoritative `outbox_required` marker), rechecks the complete required P6
parent/event/snapshot contract, rejects legacy/unverified and synthetic-corrupt sources,
then calls the existing Slice 1 factory/hash verifier. The exact approved local mapping
uses P6 contract version, Phase-6 policy version, non-binding outcome, actor/student
scope, `AUTHORITATIVE_TRANSACTION` only for durable non-binding intent persistence,
`NOT_REPLAYABLE`, fixed limitations, and exactly three null-locator/null-URI P6 evidence
references. It never calls a repository or append RPC. `append_student` and
`append_advisor` were inspected and remain fail-closed with
`UNSUPPORTED_APPEND_AUTHORITY`.

**Security blocker retained:** actual outbox RLS/grants revoke direct table access from
`PUBLIC`, `anon`, `authenticated`, and `service_role`, with no read RPC. A runtime
database projection is therefore **BLOCKED**, rather than bypassed. The new Local
Supabase mapper check reads its synthetic source pair through the local PostgreSQL owner
only to establish source/mapper evidence; it is not service-role authorization. A
separately approved bounded service-only projection RPC, with scoped grant and adversarial
tests, is the exact next prerequisite before a processor.

**Checkpoint tests:** `apps/api/.venv311/Scripts/python.exe -m pytest
apps/api/tests/test_p6_outbox_mapper.py -q` => **11 passed, 0 failed, 0 skipped in
0.79s**. With only process-local isolated Supabase values,
`apps/api/.venv311/Scripts/python.exe -m pytest
apps/api/tests/test_mock_registration_outbox_local_supabase.py -q -rs` => **4 passed,
0 failed, 0 skipped in 10.32s**. The new real-local case creates a synthetic
`STUDENT_AUTHENTICATED` P6 revision/event, maps twice, checks canonical payload/hash and
evidence, and proves no P8 ledger insert or PENDING-state transition. Full regression is
pending; no credentials were logged or recorded.

**Files changed so far:** new mapper and unit test; extended local P6 outbox test;
updated mapping approval, P6 outbox implementation, P8 persistence implementation, and
this log. P8 Slice 2 remains **NOT ACCEPTED**. **Exact next checkpoint:** execute
focused P6/P8 suites and full root backend regression with process-only local variables;
then measure files, inspect final diff, and report the service-only read-boundary blocker.

## Prompt 13A.2 -- forward-only P6 outbox replay integrity (2026-09-25, checkpoints 1--3)

**Starting branch/SHA:** `chore/p8-recovery-baseline` / `721b317fbb73e6de0cadc8201c6ea1f6849433dc`.
**Scope:** approved isolated-local corrective migration only; no P8 adapter/processor/worker/ledger append,
route, frontend, reset, production access, or historical backfill.

**Checkpoint 1 -- source finding:** matching-fingerprint replay had no durable per-revision requirement
discriminator. `created_at` is not migration membership evidence and was not used.

**Checkpoint 2 -- implementation:** added `20260925113000_enforce_p6_outbox_replay_integrity.sql`; updated
only the local outbox test, outbox implementation document, and this log. It adds
`outbox_required boolean NOT NULL DEFAULT false`; false means **LEGACY / UNVERIFIED**, not verified
pre-outbox provenance. The P6 function preserves its signature/result, `SECURITY DEFINER`, empty search
path, advisory lock, validation, and response kinds. New submitted inserts mark true and atomically insert
the source event. Marked replay validates event identity, association, type, full scope, contract version,
and exact snapshot rebuilt from immutable P6/course facts; missing or mismatched events raise explicit
integrity errors. Existing immutable history protection prevents ordinary marker updates.

**Checkpoint 3 -- local evidence:** applied only with `npx --yes supabase@2.117.0 migration up --local`;
no link/push/reset occurred. Metadata confirmed `NOT NULL DEFAULT false`, at application time **0 required /
97 legacy-unverified** rows, history ending `20260925113000`, RPC owner `postgres`, `SECURITY DEFINER`,
`search_path=""`, EXECUTE only for `postgres`/`service_role`, and outbox RLS with no client table grant.
Process-only local tests: outbox integrity **3 passed in 4.94s**; P6 persistence + local persistence +
outbox **11 passed in 7.25s**; Slice 1/2A/2B focused **65 passed in 10.76s**.

**Checkpoint 4 -- complete regression:** from the repository root with only process-local isolated local
Supabase values plus the existing local Plan ID,
`apps/api/.venv311/Scripts/python.exe -m pytest apps/api/tests -q -rs` completed **1400 passed, 0 failed,
0 skipped, 2 warnings in 511.08s (8:31)**. The two warnings are the existing FastAPI/Starlette
`httpx`/`BlockingPortal` deprecations. The prior process lacked the local Plan ID and produced 11 explicit
environment skips, so it is not used as acceptance evidence; no test marker or assertion was altered.

**Final files measured:** corrective migration **14,651 bytes**; local outbox test **27,280 bytes**;
outbox implementation record **19,678 bytes** before this final size annotation; progress record remeasured
at final handoff. No application source, route, frontend, or prior migration was modified.

**Status:** P8 Slice 2 remains **NOT ACCEPTED**. Adapter, processor, recovery, legal retention/erasure,
and production are deferred or not verified. **Exact next starting point:** human review/approval of the
forward-only P6 outbox correction before any separately authorized P8 trusted-adapter or processor slice.

## Prompt 13B.0 -- trusted adapter mapping design gate (2026-09-25)

**Scope:** documentation/source inspection only. Read the existing P6 revision/outbox contracts, P8 Slice 1
registries/factory, P8 persistence schema/safe projections, and both prior trusted-adapter drafts. No
application code, test, migration, database state, production resource, or approval state changed.

**Finding:** Prompt 13A.2's actual forward-only `outbox_required` marker and required-replay integrity gate
are present. `false` remains LEGACY/UNVERIFIED and is never a timestamp-derived pre-outbox claim. The new
approval draft freezes one proposed field mapping for `MOCK_REGISTRATION_SUBMIT`: P6 revision identity as
ledger identity; finite P8 submit/materiality/student scope; persisted P6 contract/Phase-6 policy/version
facts; non-binding validation status/outcome; `NOT_REPLAYABLE`; three immutable P6 evidence references;
and no supersession. It explicitly proposes a narrow `AUTHORITATIVE_TRANSACTION` reading for the P6 durable
non-binding transaction, not official enrollment, and documents the additive enum alternative if rejected.

**Source sufficiency:** all per-event facts are in the immutable P6 snapshot except `outbox_required`, which
must be read from the parent revision. The existing P6 projection lacks that column, requiring only a future
trusted code projection, not a schema rewrite. There is no stored binary build version; the draft maps engine
version to persisted P6 contract version and records the forward-only snapshot-version correction required
if a build identity is mandated. Proposed acceptance tests are not passing evidence.

**Files:** created `docs/p8-recovery/trusted-adapter-mapping.approval.md` (**18,917 bytes** at final
inspection); updated this log (remeasured at final handoff). **Status:** P8
Slice 2 remains NOT ACCEPTED. **Exact next starting point:** human approval/rejection of the mapping,
provenance semantics, evidence taxonomy, fixed limitation codes, and strict actor eligibility before any
separately authorized internal mapper/outbox projection implementation.

## Prompt 08 â€” P8 Runtime Slice 2B implementation checkpoint (2026-09-25)

**Starting branch/SHA:** `chore/p8-recovery-baseline` / `721b317fbb73e6de0cadc8201c6ea1f6849433dc`. **Authorized scope:** local-only trusted Python repository and service layer over the already-applied Slice 2A schema; no migration edits, routes, frontend, deterministic-engine change, or production activity. **Verified starting evidence:** the active local schema has nine migrations; Slice 1 code exposes the sole canonical SHA-256 implementation; Slice 2A stores shape-valid hashes but does not recompute them; P7 provides the exact asynchronous advisor membership/authoritative-university/active-assignment predicate. **Implementation decision:** this slice will use only server-key PostgREST calls, reconstruct typed Slice 1 entries before exposing data, validate the stored canonical hash before every projection, and use P7 authorization before advisor reads. Student authority will be derived from the authoritative student profile university, never from payload university/actor fields. Non-student scopes and system append entrypoints remain deferred.

**Implementation/test checkpoint:** created the new app.decision_trace_persistence package with finite errors, typed Supabase repository, trusted service, and allowlisted safe view/evidence types. The repository serializes through Slice 1 canonical_ledger_payload, invokes the existing atomic RPC, bounds parent reads by identity/student/university/scope, orders evidence by evidence_position, reconstructs the domain entry, and verifies its stored canonical hash before returning it. The service verifies Slice 1 hash before append and read, restricts student appends to the three approved mock-registration event types, restricts advisor append to formal guidance through P7 authorization, and rechecks P7 authorization on every advisor read. The returned evidence omits locator/URI. Tests created: apps/api/tests/test_decision_trace_persistence.py and apps/api/tests/test_decision_trace_persistence_service_local_supabase.py. Unit command apps/api/.venv311/Scripts/python.exe -m pytest apps/api/tests/test_decision_trace_persistence.py -q => **8 passed, 0 failed, 0 skipped in 0.21s**. Combined Local Supabase Slice 2 command => **13 passed, 0 failed, 0 skipped in 9.03s**. The only failed attempt was a non-test py_compile invocation with a literal PowerShell wildcard; it did not execute source code and was superseded by pytest collection/execution. P8.1 remains PARTIAL / AWAITING HUMAN APPROVAL and Slice 2 overall remains NOT ACCEPTED. **Next checkpoint:** run Slice 1 plus all Slice 2 focused checks, then the complete root regression with isolated process-only local credentials and record the actual outcome.

## Prompt 08 — P8 Runtime Slice 2B final verification (2026-09-25)

**Focused validation:** Slice 1 command apps/api/.venv311/Scripts/python.exe -m pytest apps/api/tests/test_decision_trace.py -q => **40 passed in 0.09s**. Existing Slice 2A Local Supabase command => **9 passed in 4.85s**. New Slice 2B unit command => **8 passed in 0.19s**. New Slice 2B Local Supabase command => **4 passed in 4.15s**. No focused test skipped or failed.

**Complete regression:** from repository root with only process-local local-Supabase URL/server/anon settings, the existing application service URL/server setting, and a local Plan ID, apps/api/.venv311/Scripts/python.exe -m pytest apps/api/tests -q -rs => **1393 passed, 0 failed, 0 skipped, 2 warnings in 748.93s (12:28)**. The warnings are the existing FastAPI/Starlette httpx and BlockingPortal deprecations. No skip marker or pre-existing assertion was weakened.

**Actual local verification:** database remains at exactly 9 applied migrations, with latest 20260924163500. The new service suite used synthetic local Auth users, profiles, P7 memberships, and P7 assignments; it did not use production data or credentials. It verifies real canonical append/round-trip, duplicate handling, authenticated direct evidence mutation/RPC denial, anonymous raw read denial, student and advisor authorization, assignment revocation, inactive membership, cross-tenant advisor denial, atomic rollback, and database-enforced concurrent successors. Unit MockTransport proves stored-payload tamper detection without bypassing local append-only triggers. A static scope scan found no FastAPI, APIRouter, or route declaration in the Slice 2B package/tests.

**Files created/modified and measured sizes at this checkpoint:** package __init__.py (512 bytes), errors.py (780), models.py (680), repository.py (15,790), service.py (11,295); tests test_decision_trace_persistence.py (14,572) and test_decision_trace_persistence_service_local_supabase.py (21,437); implementation document (14,131); this log (size rechecked at final delivery). Existing migration files, academic engines, routes, and frontend were not modified. **Failed attempt/fix:** the first unit test used unavailable httpx.Request.json; corrected the test to parse request.content, after which the unit suite passed. A literal PowerShell py_compile wildcard was a non-execution command error; a subsequent enumerated py_compile completed successfully.

**Status:** Slice 2B is **IMPLEMENTED / VERIFIED LOCALLY** for the stated local service boundary. P8.1 remains **PARTIAL / AWAITING HUMAN APPROVAL**. P8 Slice 2 overall remains **NOT ACCEPTED**, with production deployment, legal retention/erasure, system append actors, user-facing routes, replay execution, and production security validation still deferred or not verified. **Exact next starting point:** human review of Slice 2A/2B evidence and explicit approval before any HTTP exposure, workflow integration, or production planning.

## Prompt 07 — P8 Runtime Slice 2A implementation checkpoint (2026-09-25)

**Starting branch/SHA:** `chore/p8-recovery-baseline` / `721b317fbb73e6de0cadc8201c6ea1f6849433dc`. **Authorized scope:** local-only database persistence foundation; no Python repository/service, HTTP route, frontend, academic-engine, production deployment, or future P8 capability. **Migration preflight/application:** confirmed local API `127.0.0.1:54321`, local Postgres `127.0.0.1:54322`, and no linked hosted project. Temporary `npx --yes supabase@2.117.0` applied `20260924163500_add_decision_trace_ledger.sql` with `migration up --local` after the previous eight migrations. No reset, link, db push, or hosted resource was used.

**Implemented objects:** additive `public.decision_trace_ledger` parent, ordered `public.decision_trace_evidence` child, constraints/foreign keys/indexes, one-current-successor index, append-only triggers, and restricted `SECURITY DEFINER` RPC `public.append_decision_trace_ledger(jsonb,jsonb)`. RLS is enabled without client row policies; direct table privileges are revoked and only `service_role` has direct SELECT plus RPC EXECUTE. The RPC validates storage structure and hash shape only: it neither reproduces nor verifies Slice 1 canonical SHA-256. Slice 2B must perform canonicalization, hash verification, user authorization, and safe projections.

**Checkpoint tests:** the initial local test run was **8 passed, 1 failed** because a synthetic foreign university used nonexistent `country_code`; inspection proved the actual schema requires `country`. Only that test fixture was corrected. Rerun `apps/api/.venv311/Scripts/python.exe -m pytest apps/api/tests/test_decision_trace_persistence_local_supabase.py -q -rs` => **9 passed, 0 failed, 0 skipped in 5.27s**. Focused Slice 1 `... pytest apps/api/tests/test_decision_trace.py -q` => **40 passed in 0.23s**. The completed full regression is recorded in the following final checkpoint. **Status:** Slice 2A is IMPLEMENTED locally and TESTED to this checkpoint; P8.1 remains PARTIAL / AWAITING HUMAN APPROVAL; P8 Slice 2 overall remains NOT ACCEPTED.

## Prompt 07 — P8 Runtime Slice 2A final verification (2026-09-25)

**Full regression:** with only process-local keys for the verified isolated local stack, `apps/api/.venv311/Scripts/python.exe -m pytest apps/api/tests -q -rs` completed **1381 passed, 0 failed, 0 skipped, 2 warnings in 1242.45s (20:42)**. The two warnings are existing FastAPI/Starlette deprecations. The complete suite includes the 9 executed P8 Slice 2A Local Supabase tests; no skip condition or existing test was changed.

**Actual database inspection:** local migration history contains `20260924163500`; the ledger has 28 columns and evidence has 7. Both have RLS enabled and no permissive policies. The ledger has the expected primary key, university/student indexes, and partial unique `idx_decision_trace_ledger_one_successor`. The two append-only triggers and supersession validation trigger are installed. Direct table grants are SELECT-only to `service_role`; only `service_role` (and database owner `postgres`) can execute the append RPC. The RPC is confirmed SECURITY DEFINER with fixed `search_path=pg_catalog, public`.

**Created files and measured sizes at final verification:** `supabase/migrations/20260924163500_add_decision_trace_ledger.sql` (**17,645 bytes**), `apps/api/tests/test_decision_trace_persistence_local_supabase.py` (**15,480 bytes**), and `docs/decision-trace-persistence-implementation.md` (**8,438 bytes**). This persistent log was updated at the checkpoint and final verification. No existing migration, application engine, frontend, canonical Slice 1 code, or production resource was modified. Pre-existing untracked recovery material, `AGENTS.md`, archive, and the narrow existing `.gitignore` edit remain preserved.

**Security acceptance evidence:** actual local tests verify service append/atomicity, structural validation, direct authenticated raw row and RPC denial, cross-student and analyst raw row denial, malformed evidence rejection, duplicate identity rejection, supersession predecessor/hash/scope validation, concurrent successor exclusion, and trigger-enforced immutable parent/evidence history. The database does not recompute canonical hashes, and end-user student/advisor read authorization is not yet present: these are Slice 2B requirements, not passed guarantees. PostgreSQL owner/superuser bypass and retention/erasure process remain limitations.

**Acceptance/status:** P8.1 remains **PARTIAL / AWAITING HUMAN APPROVAL**. Slice 1 remains **VERIFIED** by its 40 focused tests. Slice 2A is **IMPLEMENTED / TESTED LOCALLY**. P8 Runtime Slice 2 overall remains **NOT ACCEPTED** and is not production-ready. **Exact next starting point:** human review of this local Slice 2A foundation and its remaining policy approvals, followed only by explicit approval for Slice 2B's trusted Python canonical-hash verification/persistence service and role-safe retrieval projections with additional Local Supabase adversarial tests.

## Project identity and immutable architecture

- **Product:** Morshidi, an Arabic-first academic intelligence operating system.
- **Pilot:** Zarqa University, Artificial Intelligence, Study Plan 12.
- **Architectural invariant:** **AI EXPLAINS — DETERMINISTIC RULES DECIDE.**
- LLM/RAG may explain, summarize, retrieve governed policy evidence, and cite sources. They must not independently decide eligibility, prerequisites, progress, graduation, semester validity, or degree-path correctness.
- Student data remains owner-scoped. Advisor access requires the established active advisor role, same-university boundary, and active explicit student assignment. Analysts have no student-level trace access.

## Audit record — 2026-09-24

### Audit scope

This is a reconstruction-only audit. No runtime code, tests, policies, migrations, deployment configuration, or database state was changed.

### Local checkout and upstream Git state

| Item | Verified result |
| --- | --- |
| Local directory | `D:\project\morshidi\Morshidi-main` |
| Local `.git` | **Absent**; this directory is not a Git worktree. `git status --short`, `git branch --show-current`, and `git rev-parse HEAD` all return `fatal: not a git repository`. |
| Local staged/unstaged/untracked state | **Not determinable** without Git metadata. Do not interpret this as a clean worktree. |
| Upstream | `https://github.com/mohannad-121/Morshidi.git` |
| Upstream branch / HEAD | `main` at `721b317fbb73e6de0cadc8201c6ea1f6849433dc` (`fix: restore student dashboard API connectivity`) |
| Upstream audit clone status | Clean at the verified upstream HEAD. It was created outside this project solely for read-only history inspection. |

The sibling archive `D:\project\morshidi\Morshidi-main.zip` contains no `.git` entries and no Slice 2 files. It cannot restore Git history or missing Slice 2 work.

### Reconstructed architecture

| Area | Verified implementation evidence |
| --- | --- |
| API | FastAPI application in `apps/api/app/main.py`; routers for health, eligibility, student, advisor, mock registration, institutional demand/intelligence, and advisor copilot. |
| Academic domain | Separate packages for catalog, rules/eligibility, progress, recommendations, semester planning, degree paths, student intelligence, digital twin, decision intelligence, mock registration, institutional intelligence, and advisor tooling. |
| Persistence | Supabase/PostgREST repository adapters and eight migrations through P7.4. Current migration tail is `20260923150000_add_advisor_authorization_persistence.sql`. |
| Frontend | Next.js/TypeScript app with Supabase SSR/client session boundary, protected student layout, `AuthenticatedApiClient`, login/sign-out, and profile/progress dashboard. |
| Deployment/configuration | API and web `.env.example` files, `apps/web/next.config.ts`, and `supabase/config.toml` exist. No `render.yaml`, `vercel.json`, Dockerfile, or compose file exists in the checked tree. |

### Phase status

| Scope | Status | Evidence and limitation |
| --- | --- | --- |
| Core academic P1–P10.7A | REPORTED | Relevant packages, migrations, routes, tests, and validation documentation are present. This audit did not rerun the full suite. |
| Advanced governance P1–P7.5 | REPORTED | Relevant domains and P7.4/P7.5 code/documents are present. No complete acceptance rerun occurred in this environment. |
| P8.1 policy gate | PARTIAL | The top-level P8 policy is nonempty; six required companion documents are zero-byte. |
| P8 Slice 1 — Decision Trace Ledger Domain Core | PARTIAL | Corrective code and 40 focused test functions exist and were historically restored by `d217f37`; focused tests are blocked locally because pytest is unavailable. |
| P8 Slice 2 — Decision Trace Ledger Persistence & Security | NOT ACCEPTED | No persistence package, tests, policies, implementation note, or migration exists at local disk or upstream `main`. No RLS/RPC/integration evidence exists. |
| Student dashboard behavior | PARTIAL | Static inspection verifies protected routes, Supabase Auth, bearer-token API client, academic profile/progress calls, retry/error handling, and sign-out. Frontend tests were not runnable locally. |
| Student visual identity | PARTIAL | Current UI is dark/zinc with emerald accents. Git history available on `main` does not contain a prior yellow/gold dashboard; the pre-dashboard UI also used emerald. Existing brand asset: `apps/web/public/brand/morshidi-guide.png` (5,661,566 bytes). |

### P8.1 documentation verification

Exact local sizes, which match upstream `main` blob sizes:

| File | Bytes | Recoverability |
| --- | ---: | --- |
| `docs/p8-grounded-knowledge-decision-trace-policy.md` | 9,121 | Present and readable. |
| `docs/decision-trace-ledger-policy.md` | 0 | Not recoverable from available Git history: introduced empty in `161d0ad`. |
| `docs/institutional-policy-retrieval-policy.md` | 0 | Same. |
| `docs/academic-explainability-graph-policy.md` | 0 | Same. |
| `docs/change-impact-policy.md` | 0 | Same. |
| `docs/institutional-ai-query-policy.md` | 0 | Same. |
| `docs/p8-grounded-knowledge-test-matrix.md` | 0 | Same. |

`161d0ad` (`docs: establish P8 grounded knowledge policy contracts`) introduced all six empty files. There are no other remote branches, tags, or prior path commits holding their content. Do not fabricate or silently replace these policy contracts.

### P8 Slice 1 verification

Verified nonempty files:

| Path | Bytes |
| --- | ---: |
| `apps/api/app/decision_trace/__init__.py` | 1,414 |
| `apps/api/app/decision_trace/canonical.py` | 2,445 |
| `apps/api/app/decision_trace/models.py` | 2,572 |
| `apps/api/app/decision_trace/registries.py` | 4,836 |
| `apps/api/app/decision_trace/replay.py` | 2,206 |
| `apps/api/app/decision_trace/validation.py` | 6,240 |
| `apps/api/tests/test_decision_trace.py` | 7,749 |
| `docs/decision-trace-domain-implementation.md` | 3,246 |

Static inspection confirms frozen/slot dataclasses, finite registries and materiality mapping, canonical JSON serialization, SHA-256 integrity hashing, UTC/scope/hash validation, supersession with a new identity and previous hash, exact/current/not-replayable modes, and metadata redaction. The focused file contains 40 tests (`P8_TRACE_001`–`P8_TRACE_040`); this is a collection count, **not** a passing result.

Git history confirms `e5eb454` added zero-byte Slice 1 files and `d217f37` subsequently added 373 real lines across the package, test, and implementation document. No persistence service, route, or main-application wiring imports this P8 core yet, consistent with its documented domain-only scope.

### P8 Slice 2 exact state

All of the following are absent on local disk and upstream `main`:

- `apps/api/app/decision_trace_persistence/`
- `apps/api/tests/test_decision_trace_persistence.py`
- `apps/api/tests/test_decision_trace_persistence_local_supabase.py`
- `docs/decision-trace-persistence-implementation.md`
- `docs/decision-trace-persistence-policy.md`
- `supabase/migrations/20260924163500_add_decision_trace_ledger.sql`

Consequently, none of append-only enforcement, trace/evidence schema, student ownership, assigned-advisor authorization, same-university isolation, analyst aggregate-only visibility, RLS, security-definer RPC boundary, evidence authorization, integrity verification at persistence time, supersession-chain persistence, replay persistence, or cross-tenant resistance is implemented or verified for Slice 2.

### Test baseline

| Command / check | Result | Actual outcome |
| --- | --- | --- |
| `cd apps/api; python -m pytest tests/test_decision_trace.py -q` | BLOCKED | `No module named pytest` from Python 3.10. |
| `cd apps/api; python -m pytest tests -q` | NOT RUN | Not attempted after pytest dependency gate failed. |
| `cd apps/web; npm test` | NOT RUN | `apps/web/node_modules` is absent; dependencies were not installed during a reconstruction-only audit. |
| Local Supabase tests | NOT RUN | `supabase` CLI is absent. Existing tests require local-only variables such as `MORSHIDI_LOCAL_SUPABASE_URL`, `MORSHIDI_LOCAL_SUPABASE_SERVER_KEY`, and, where applicable, `MORSHIDI_LOCAL_SUPABASE_ANON_KEY`; no values were read or exposed. |

There are 53 backend test files, 10 existing `*local_supabase.py` files, and 6 frontend test files. No P8 Slice 2 test file exists.

### Risks, discrepancies, and preservation warnings

1. The local directory lacks `.git`; do not run reset/clean, and do not treat its state as version-controlled until a proper clone or Git metadata recovery is explicitly authorized.
2. The root `README.md` is materially outdated: it states that database infrastructure, rules engine, student data, recommendations, and AI are not implemented, contradicting the code and migrations.
3. Six P8 companion policies/test matrix are committed empty upstream. Their missing content blocks policy-conformant Slice 2 design and acceptance.
4. Slice 2 is absent, not merely unaccepted. No claim about RLS, RPC, immutability, or database authorization may be made.
5. Existing local-Supabase tests for earlier phases are opt-in and skip when their required local-only variables are absent. Unit tests cannot prove database RLS/security-definer behavior.
6. The desired yellow/gold historical dashboard cannot be recovered from currently available Git history; preserve dashboard functionality and use an approved design reference before visual-only work.

### Next recommended smallest coherent slice

**Blocking prerequisite:** recover the six authoritative P8 companion policy/test-matrix documents from an approved external source (or have the product/security owner approve new policy authoring) and establish a real Git worktree. Do not invent their requirements.

**Then, and only after explicit human approval:** define and implement the smallest P8 Slice 2 persistence/security slice: a canonical trace/evidence schema and append-only service-side append boundary, with an explicit RLS/RPC access matrix. It must include real local-Supabase adversarial tests for direct client mutation denial, student-only access, advisor assignment plus same-university access, analyst denial of individual traces, cross-tenant denial, evidence inheritance, tamper detection, and supersession/replay behavior.

**Acceptance criteria:** policy source approved; migration replayed only against local Supabase; tables/RLS/functions/grants manually inspected; all focused unit tests pass; Slice 2 local security tests pass with zero skips; backend regression suite passes; file sizes and diffs checked; no production migration, commit, push, or staging occurs without approval.

## Contract-audit checkpoint — 2026-09-24

**P8 contract audit:** On `chore/p8-recovery-baseline` at `721b317fbb73e6de0cadc8201c6ea1f6849433dc`, reviewed all seven recovery documents against the P8 umbrella policy, Slice 1 domain code/tests, P6 mock-registration RLS/threat/RPC precedent, P7 advisor authorization policy/service/migration, and capability roadmap/matrix. The drafts preserve deterministic-engine precedence, no-write boundaries, owner/advisor/tenant predicates, analyst individual-data denial, and Slice 1's non-authorizing SHA-256 contract. The umbrella policy's assertion that companion contracts/matrix exist conflicts with six actual zero-byte files and cannot support original-detail claims. Schema, event scope, evidence lifecycle, service/RPC design, retention/deletion, exact-replay artifact retention, graph registry, RAG source governance, impact semantics, and institutional metric catalog remain **OPEN CONTRACT / REQUIRES HUMAN APPROVAL**. P6/P7 are precedents only, not approved P8 SQL. Database guarantees remain **NOT VERIFIED** without local Supabase. P8.1 remains **PARTIAL / AWAITING HUMAN APPROVAL** and Slice 2 remains **NOT ACCEPTED**. Next: decision register, smallest-slice plan, and human review package.

## Security-architecture review checkpoint — 2026-09-24

**P8 Slice 2 security review:** Created `docs/p8-recovery/contract-decision-register.md`, `slice2-implementation-plan.draft.md`, and `human-review-package.md`; all are proposal/review material, not approved contracts. The audit separates Category A ledger blockers from Category B later P8 work. Category A records: event scope, parent/evidence schema, immutability/retention, owner/advisor viewer authorization, service/RPC boundary, evidence projection, hash verification, supersession semantics, replay retention, and RLS/grant acceptance. Category B defers RAG source governance, graph registry, impact semantics, and institutional metric catalog. Actual security finding: application authorization and database guarantees are distinct; P7 adviser predicate and P6 RLS/RPC patterns are reusable evidence only, while P8 tables/RLS/RPC do not exist. Required real database tests include direct insert/update/delete denial, student and tenant isolation, advisor assignment/revocation, analyst/evidence denial, function EXECUTE restrictions, tamper, supersession, and replay scope. `git diff --check` passed; no application code, migrations, existing tests, production resources, or credentials changed. **Status:** P8.1 remains **PARTIAL / AWAITING HUMAN APPROVAL**; P8 Slice 2 remains **NOT ACCEPTED**. **Exact next starting point:** human reviews and resolves Category A decisions, approves policy promotion and local-Supabase security prerequisites, then authorizes a separate Slice 2 implementation task.

## Chronological development log

| Date | Phase | Entry |
| 2026-09-25 | Prompt 13A.1 / transactional outbox review — final verification | **Scope preserved:** review hardening and evidence only. No migration/history rewrite, adapter, processor, claim/retry operation, worker, HTTP route, frontend, P8 append, production access, reset, commit, push, merge, or PR. **Finding A — BLOCKED:** the current replay path cannot reliably distinguish a missing-event post-outbox `SUBMITTED` revision from pre-outbox history because no per-revision migration-membership marker exists. `created_at` is not safe evidence. A forward-only, separately approved additive migration is required: immutable `outbox_required` false for known prior history, true only inside future P6 submit inserts, plus replay-time exact event verification for marked rows. This cannot prove unknown rows in the already-created interval and was neither created nor applied. **Finding B — VERIFIED LOCALLY:** the strengthened isolated test captures a valid prior revision/event, injects a uniquely named temporary local outbox failure for a new submit, removes test objects in `finally`, and proves no new P6 revision, course, or event while prior revision/event are unchanged. **Finding C — VERIFIED LOCALLY:** every existing snapshot field is reloaded from immutable P6 revision/course data and compared: identities, owner/university/major/plan/plan version/period/revision, lifecycle/validation/fingerprint, intent/source/policy/contract/period/notice versions, actor, null progress reference, canonical course identity/order, and timestamp instant. JSON/scope checks are database-enforced; P6 function construction is trusted; a future processor must independently recheck source before construction. No unapproved P8 mapping was added. **Finding D — VERIFIED CURRENT DENIAL / DEFERRED:** RLS enabled; no policies and no anon/authenticated/service-role direct outbox privileges; existing P6 RPC remains `postgres`-owned `SECURITY DEFINER`, fixed empty search path, service-role-only EXECUTE, and has no P8 append. Future atomic claim, lease token, conditional completion, safe retry, and expired-lease recovery remain design-only; no broad grant/processor exists. **Actual tests:** focused outbox **2 passed in 3.64s**; P6 persistence + outbox **3 passed in 5.09s**; Slice 1/2A/2B focused **65 passed in 10.33s**; root `apps/api/.venv311/Scripts/python.exe -m pytest apps/api/tests -q -rs` with process-local isolated values **1399 passed, 0 failed, 0 skipped, 2 warnings in 513.14s (8:33)**. Existing warnings are FastAPI/Starlette test-client deprecations. **Acceptance:** rollback/snapshot tests and current table/RPC denial are IMPLEMENTED / VERIFIED LOCALLY; replay-missing-event distinction is BLOCKED; P8 Slice 2 remains NOT ACCEPTED. **Exact next starting point:** human decision on the forward-only `outbox_required` corrective contract and its known historical limitation before any migration; after that, separately authorize a trusted adapter/processor slice only when P8 mapping and restricted claim/complete contracts are approved. |
| 2026-09-25 | Prompt 13A.1 / transactional outbox review — checkpoint | **Scope:** local-only review hardening/verification; no adapter, processor, worker, migration, route, frontend, P8 append, reset, production access, commit, push, merge, or PR. **Starting branch/SHA:** `chore/p8-recovery-baseline` / `721b317fbb73e6de0cadc8201c6ea1f6849433dc`. **Finding A:** reviewed actual replay branch and current schema. It returns `IDEMPOTENT_REPLAY` without explicitly requiring an event. A current row lacking an event cannot be reliably classified as pre-outbox history or post-migration corruption: `created_at` is not authoritative migration membership and schema history does not tag rows. This remains **BLOCKED**, not silently accepted. The documented separate, approval-required forward correction is an immutable `outbox_required` revision marker set only by a revised P6 insert function, plus replay-time exact-event verification. It cannot conclusively repair the unknown interval between the already-applied outbox migration and a later corrective migration. No migration was created/applied. **Findings B/C:** strengthened `test_mock_registration_outbox_local_supabase.py` to capture a valid prior revision/event, force an isolated temporary outbox insert failure for the next uniquely identified submission, clean it in `finally`, then prove no new revision/course/event and unchanged prior records. It now reloads P6 revision data and compares every current snapshot field including identity, scope, plan version, validation/fingerprint, version arrays, canonical course order, null progress reference, and UTC timestamp. **Test:** `... pytest apps/api/tests/test_mock_registration_outbox_local_supabase.py -q -rs` => **2 passed, 0 failed, 0 skipped in 3.64s**. **Finding D:** actual local inspection remains RLS enabled, zero outbox policies, no direct outbox privilege for anon/authenticated/service_role, P6 function owner `postgres`, `SECURITY DEFINER`, fixed empty search path, service-role-only EXECUTE, and no direct P8 append. Future restricted claim/lease/conditional completion/retry/recovery is documented but **DEFERRED**; no broad grant or processor exists. **Status:** rollback and snapshot current-contract evidence are VERIFIED LOCALLY; replay-missing-event historic distinction is BLOCKED pending human decision/additive migration; P8 Slice 2 remains NOT ACCEPTED. **Next:** run combined focused and full root regression with local-only process configuration, record actual counts, then present the exact forward-integrity approval gate. |
| 2026-09-25 | Prompt 13A / P6 transactional outbox foundation — final verification | **Starting branch/SHA:** `chore/p8-recovery-baseline` / `721b317fbb73e6de0cadc8201c6ea1f6849433dc`. **Implemented scope:** one local-only additive migration, `20260925103000_add_p6_decision_trace_outbox.sql`; one real Local Supabase test file, `apps/api/tests/test_mock_registration_outbox_local_supabase.py`; `docs/decision-trace-outbox-implementation.md`; and this log. No P8 adapter/ledger append, processor/worker/claim loop, route, frontend, academic engine, production action, reset, deployment, staging, commit, push, merge, or PR. **Database verification:** local history has 10 rows ending in `20260925103000`; `decision_trace_outbox` exists with 24 columns, 21 constraints, restrictive revision FK/unique identity, two immutable/delete triggers, RLS enabled, zero row policies, and table privileges only for local DB owner `postgres`. `anon`, `authenticated`, and `service_role` have no direct outbox grant. `persist_mock_registration_revision` remains owner `postgres`, `SECURITY DEFINER`, `search_path = ''`, has no anon/authenticated EXECUTE, does have service-role EXECUTE, and its current actual definition inserts `decision_trace_outbox`. **Actual semantics:** a new `SUBMITTED` P6 revision atomically writes immutable revision header, canonical courses, and exactly one pending event with `event_id = revision_id`; a required outbox failure rolls all three back. The source snapshot contract is `1.0` and uses only trusted P6 transaction facts; it has no P8 hash/outcome/evidence/provenance mapping. Idempotent replay preserves its result and adds no duplicate event. Historical pre-outbox replay preserves legacy idempotency and never backfills. P6 withdrawals are intentionally outside the sole approved `MOCK_REGISTRATION_SUBMIT` event scope. **Tests:** `... pytest apps/api/tests/test_mock_registration_outbox_local_supabase.py -q -rs` => **2 passed, 0 failed, 0 skipped in 3.84s**. P6 persistence + outbox => **3 passed, 0 failed, 0 skipped in 4.65s**. Slice 1/Slice 2A/Slice 2B focused command => **65 passed, 0 failed, 0 skipped in 11.58s**. Final root command, with local-only generated values in process memory, `apps/api/.venv311/Scripts/python.exe -m pytest apps/api/tests -q -rs` => **1399 passed, 0 failed, 0 skipped, 2 warnings in 511.87s (8:31)**. The warnings are existing Starlette test-client/BlockingPortal deprecations. A first regression wrapper did not return a terminal summary although its child continued; it is explicitly not counted. The final run used only temporary output/JUnit files outside the repository and is the evidence recorded here. **Security evidence:** the new real-local tests prove direct client outbox SELECT/INSERT/UPDATE/DELETE and unauthorized P6 RPC denial; parent FK enforcement; source-field immutability; exact snapshot/scope values; idempotency/concurrent same-fingerprint handling; and a temporary test-only, always-removed outbox trigger proves database rollback. **Acceptance:** foundation is **IMPLEMENTED / VERIFIED LOCALLY**. P8 Slice 2 overall remains **NOT ACCEPTED**; production security, retention/erasure, P8 evidence/provenance/field mappings, trusted adapter, processor, operational state mutation authority, and production deployment remain deferred/not verified. **Exact next starting point:** obtain explicit human approval for the remaining P6→P8 mapping gates, then separately authorize the trusted internal adapter/processor slice with bounded service-only claim/complete operations and new end-to-end local recovery tests. |
| 2026-09-25 | Prompt 13A / P6 transactional outbox foundation — migration checkpoint | **Scope:** local-only P6 durable event foundation for future `MOCK_REGISTRATION_SUBMIT` tracing; no P8 adapter, P8 ledger append, processor, worker, route, frontend, academic decision change, production action, reset, commit, push, merge, or PR. **Starting branch/SHA:** `chore/p8-recovery-baseline` / `721b317fbb73e6de0cadc8201c6ea1f6849433dc`. **Preflight:** Docker Engine 29.8.0 is operational; local Supabase API is `127.0.0.1:54321`, database is `127.0.0.1:54322`, and the verified local-only CLI is `npx --yes supabase@2.117.0`. The stack was running (Vector restart remains a known observability issue, while DB/API/Auth were healthy). **Implemented migration:** `supabase/migrations/20260925103000_add_p6_decision_trace_outbox.sql`, applied only with `migration up --local`, is the tenth local migration. It adds `public.decision_trace_outbox`, RLS with no direct client/service-role grants, immutable source-event trigger protection, delete prevention, source/parent/scope/finite-state constraints, and indexes. It redefines the existing same-signature `persist_mock_registration_revision` as `SECURITY DEFINER SET search_path = ''`, preserving validation/advisory lock/return rows/idempotency/grants while inserting exactly one outbox event inside the same transaction for a newly `INSERTED` **SUBMITTED** revision. Withdrawals remain outside this first approved submit-event scope. **Snapshot:** contract `1.0` contains only P6 transaction facts (revision/scope/status/fingerprint/version fields, timestamp, canonical selected courses); it contains no P8 hash, outcome, evidence, or provenance mapping. `event_id = revision_id`; the parent FK is restrictive and one outbox event per revision is unique. **Compatibility:** an idempotent replay of a historical pre-outbox revision retains legacy result semantics and is never backfilled. **Initial local tests:** `apps/api/.venv311/Scripts/python.exe -m pytest apps/api/tests/test_mock_registration_outbox_local_supabase.py -q -rs` => **2 passed, 0 failed, 0 skipped in 3.84s**. The tests used synthetic local users/periods. They prove new submit header/course/outbox atomicity, rollback when a temporary local test-only outbox insert trigger raises (removed in `finally`), replay/concurrent uniqueness, no event for invalid/conflict, direct client/unauthorized-RPC denial, FK/immutability behavior, exact snapshot sources, and legacy replay without backfill. No test disables a production-style trigger or changes persistent security. **Next:** inspect local DB metadata/function definition/grants, create actual implementation documentation, run focused existing P6/P8 tests and full root regression, then record final counts and remaining processor/adapter gates. |
| 2026-09-25 | Prompt 12 / trusted adapter and durable outbox design gate | **Scope:** source inspection and design documentation only for the approved local `MOCK_REGISTRATION_SUBMIT` candidate; no adapter/outbox implementation, migration, test change, route, frontend work, database action, production access, commit, push, merge, or PR. **Starting branch/SHA:** `chore/p8-recovery-baseline` / `721b317fbb73e6de0cadc8201c6ea1f6849433dc`. **Inspected sources:** `AGENTS.md`, this log, Prompt 11 adapter contract, P8 domain/persistence docs and actual models/registries/canonical factory/repository/service/migration, plus P6 Mock Registration route/service/domain/persistence models/repository/tests and `20260922134625_add_mock_registration_persistence_security.sql`. **Verified P6 transaction:** `public.persist_mock_registration_revision` is `SECURITY DEFINER`, has `search_path = ''`, service-role-only execution, a scoped advisory transaction lock, idempotent replay by revision-slot/fingerprint, immutable revision/course inserts, and statement rollback on error. It is atomic for P6 only: current P8 append is a separate RPC, so Python `try/except` cannot make a committed P6 revision and P8 trace atomic. **Design finding:** a same-P6-transaction outbox insert is feasible as a separately approved additive migration; its insertion failure would intentionally roll back P6 submit, an explicit human decision. **Created:** `docs/p8-recovery/trusted-adapter-outbox-design.draft.md`, marked `STATUS: DRAFT — AWAITING HUMAN APPROVAL`; it proposes a one-to-one revision/event identity, minimum immutable P6 adapter-input snapshot plus stable reference (reference-only recovery is unsafe), bounded server-only claim/lease/retry/completion, exact P8 duplicate payload/hash verification, crash recovery, security boundaries, and 18 proposed unit/local integration tests. **Approved local directions preserved:** `persisted.revision_id` is the proposed ledger identity; `NOT_REPLAYABLE` is the initial replay direction; the trace describes a non-binding declared intent only. **Unresolved gates:** source-engine/version and P8 policy mapping; source-version representation; validation-status/outcome-reference semantics; typed evidence taxonomy; honest provenance classification (especially `AUTHORITATIVE_TRANSACTION`); student subject-scope convention; snapshot retention/erasure; and retry/lease/error/RPC grant policy. **Tests:** NOT RUN by design; historic **1397 passed, 0 failed, 0 skipped** remains prior evidence only. **Status:** P8.1 remains PARTIAL / AWAITING HUMAN APPROVAL and P8 Slice 2 remains NOT ACCEPTED. **Exact next starting point:** explicit human approval of the recorded design gates, then a separately authorized local-only P6 transactional-outbox foundation migration and focused real Local Supabase acceptance tests; do not implement the adapter before that approval. |
| 2026-09-25 | Prompt 11 / trusted deterministic result-to-ledger adapter contract | **Scope:** source inspection and contract drafting only; no adapter, route, frontend, test, migration, or production operation. **Starting branch/SHA:** `chore/p8-recovery-baseline` / `721b317fbb73e6de0cadc8201c6ea1f6849433dc`. **Source inspected:** P8 Slice 1 envelope/canonicalization/validation/registries; hardened P8 persistence repository/service; authenticated P6 Mock Registration route; `MockRegistrationStudentService.submit`; `SupabaseAcademicContextLoader.load_owner_context` and `domain_context`; `validate_registration_intent`; P6 typed persistence command/result/revision/repository; P6 migration idempotency RPC; relevant P6/P8 tests. **Selected proposed first workflow:** a successful, authenticated, deterministic, non-binding Mock Registration submit, only after P6 has persisted and reloaded the exact `PersistedIntentRevision`. It maps to the existing material `MOCK_REGISTRATION_SUBMIT`; it is never official enrollment or an academic-state decision. **Trust finding:** P6 has a trustworthy call graph and typed in-process execution objects, but `StudentIntentResult` alone is insufficient and `submit(owner: str, ...)` does not by itself prove transport Auth. The future internal adapter must bind `CurrentUser`, `AcademicContextSnapshot`, `ValidatedIntent`, `PersistRevisionResult`, `PersistedIntentRevision`, and target period; it must not accept any caller-provided `CanonicalLedgerEntry`, hash, outcome, engine, provenance, or evidence. Existing hardened public append methods remain disabled. **Mapping gaps requiring approval:** controlled engine/policy/version identifiers, P6→P8 status map, outcome-reference semantics, individual subject-scope-ID convention, evidence reference taxonomy/order, non-binding provenance enum selection, initial replay classification, and durable recovery after a P6 commit/P8 failure. P6 and P8 are separate RPC transactions; no cross-RPC atomicity or outbox exists. **Created file:** `docs/p8-recovery/trusted-result-ledger-adapter-contract.draft.md` (**21,793 bytes** at creation), marked `STATUS: DRAFT — AWAITING HUMAN APPROVAL`; it provides all canonical-field mappings, trust boundaries, idempotency/failure design, and 16 proposed acceptance scenarios. **Tests:** NOT RUN by design; no executable source changed. **Security:** no local/hosted database action, credential access, schema change, or production operation occurred. **Status:** P8.1 remains PARTIAL / AWAITING HUMAN APPROVAL; P8 Slice 2 remains NOT ACCEPTED. **Exact next starting point:** human review and explicit resolution of the six adapter decisions in the new draft, especially durable P6→P8 failure recovery; only then authorize the smallest internal P6-submit adapter and its unit/Local Supabase acceptance tests. |
| 2026-09-25 | Prompt 10 / P8 Slice 2B hardening final verification | **Focused results:** Slice 1 `apps/api/.venv311/Scripts/python.exe -m pytest apps/api/tests/test_decision_trace.py -q` => **40 passed in 0.09s**; hardening unit command `... pytest apps/api/tests/test_decision_trace_persistence.py -q` => **11 passed, 0 failed, 0 skipped in 0.55s**; actual Local Supabase Slice 2A+2B command `... pytest apps/api/tests/test_decision_trace_persistence_local_supabase.py apps/api/tests/test_decision_trace_persistence_service_local_supabase.py -q -rs` => **14 passed, 0 failed, 0 skipped in 13.99s**. **Full regression:** from the repository root, with generated isolated-local API/server/anon values, local study plan, and the existing application Supabase values held in process memory only, `apps/api/.venv311/Scripts/python.exe -m pytest apps/api/tests -q -rs --junitxml=<temporary-outside-worktree>` => **1397 passed, 0 failed, 0 errors, 0 skipped in 632.802s**. The JUnit report does not encode pytest's terminal warning summary, so this final run does not claim an exact warning count; the earlier baseline's two deprecation warnings remain historical rather than inferred. Two initial full-run attempts were stopped after this environment's command wrapper yielded partial output without a retrievable final result; they made no source/database changes. The final controlled run is the recorded evidence. **Final source checks:** no `APIRouter` or route decorator exists in the persistence package; compileall succeeded; no applied migration was modified; no reset, hosted connection, production access, route, frontend, or academic-engine change occurred. `git diff --check` has no whitespace error (only the pre-existing `.gitignore` LF/CRLF warning). **Measured Prompt 10 artifacts:** package `__init__.py` 520 bytes, models 987, repository 16,518, service 8,723; unit test 19,093; local service test 23,106; persistence implementation document 19,647 at final update; progress log rechecked before delivery. **Status:** P8.1 remains PARTIAL / AWAITING HUMAN APPROVAL. Slice 2B hardening is IMPLEMENTED / VERIFIED LOCALLY only. P8 Slice 2 overall remains NOT ACCEPTED. **Exact next starting point:** obtain explicit human approval for a concrete, authoritative deterministic-result-to-ledger adapter (including derived outcome/provenance/evidence mapping and its integration tests) before any route or user-facing exposure; do not accept complete caller-supplied ledger envelopes. |
| 2026-09-25 | Prompt 10 / P8 Slice 2B hardening checkpoint | **Objective:** close source-review trust-boundary and safe-projection gaps locally, without HTTP/frontend/migration changes. **Starting branch/SHA:** `chore/p8-recovery-baseline` / `721b317fbb73e6de0cadc8201c6ea1f6849433dc`. **Confirmed defect:** a canonical hash proved integrity but did not prove a user-supplied academic outcome, engine, provenance, or evidence came from a deterministic workflow. No approved P6 result-to-ledger adapter exists and Mock Registration integration was out of scope. **Implemented local boundary:** `DecisionTraceService.append_student` and `append_advisor` now reject every user-submitted envelope with `UNSUPPORTED_APPEND_AUTHORITY` before persistence; future internal persistence requires a separately approved trusted deterministic-result adapter. Service read methods now take only `app.core.auth.CurrentUser`, the existing `get_current_user` dependency result, so a raw UUID/string is rejected and cannot serve as a transport authentication substitute. **Visibility/projection:** student reads permit only stored `STUDENT_SAFE`; P7-authorized advisors permit `STUDENT_SAFE`/`ADVISOR_SAFE`; ambiguous/restricted classifications fail closed. The returned local projection is reduced to finite/stable metadata only (ledger ID, type, status, timestamp, replay status); scope, tenant/student IDs, engine/policy/source fields, outcome/provenance/limitations, and all evidence are omitted pending approved disclosure policy. **Repository:** append rejects more than 1,000 evidence items; reads request 1,001 and fail closed on an extra row. Evidence is stored/read in Slice 1 canonical position order; unit and local tests cover Unicode ordering. **Files changed so far:** `apps/api/app/decision_trace_persistence/{__init__.py,models.py,repository.py,service.py}`, `apps/api/tests/{test_decision_trace_persistence.py,test_decision_trace_persistence_service_local_supabase.py}`, `docs/decision-trace-persistence-implementation.md`, and this log. No applied migration, academic engine, route, frontend, or production resource changed. **Checkpoint tests:** unit `... pytest apps/api/tests/test_decision_trace_persistence.py -q` => **11 passed, 0 failed, 0 skipped in 0.66s**. Actual isolated Local Supabase `... pytest apps/api/tests/test_decision_trace_persistence_local_supabase.py apps/api/tests/test_decision_trace_persistence_service_local_supabase.py -q -rs` => **14 passed, 0 failed, 0 skipped in 13.99s**. The first local wrapper attempt passed quoted `supabase status -o env` values to the URL and produced `httpx.UnsupportedProtocol`; only the process-local parser was corrected, then tests passed. **Security:** no local reset or migration was performed; credentials stayed process-only. **Status:** P8.1 remains PARTIAL / AWAITING HUMAN APPROVAL; Slice 2 overall remains NOT ACCEPTED. **Exact next starting point:** run focused Slice 1 and root regression with isolated process-only environment, then obtain human approval for a concrete deterministic-result-to-ledger adapter before any HTTP exposure. |
| --- | --- | --- |
| 2026-09-24 | Reconstruction / P8 audit | Created this record. Verified upstream `main` at `721b317fbb73e6de0cadc8201c6ea1f6849433dc`; identified local no-Git checkout, six committed zero-byte P8 files, corrected nonempty Slice 1, and entirely absent Slice 2. No runtime or policy files were modified. Tests are blocked/not run as recorded above. Next starting point: recover authoritative P8 companion policy content and a proper Git worktree, then seek explicit approval for Slice 2. |
| 2026-09-24 | Environment baseline / no implementation slice | **Objective:** establish an isolated development branch and execute the existing baseline without changing application behavior. **Starting SHA:** `721b317fbb73e6de0cadc8201c6ea1f6849433dc`. **Workspace:** `D:\project\morshidi\Morshidi-main\Morshidi-workspace`, a verified Git worktree with `origin` set to the project GitHub repository. Created and activated `chore/p8-recovery-baseline`; no commit, push, merge, stage, or pull request occurred. **Files changed:** added root `AGENTS.md` (2,279 bytes at verification); appended this log entry to this pre-existing untracked file. No runtime code, policy, migration, test, deployment, or package-lock file changed. **Environment:** Python 3.10.11 / pip 26.1.2 is incompatible with pinned `websockets==17.1`; a `.venv` creation/install attempt was blocked without changing requirements. Created ignored local `apps/api/.venv311` using Python 3.11.9, then `apps/api/.venv311/Scripts/python.exe -m pip install -r requirements.txt` succeeded with pinned versions. `apps/web/npm ci` succeeded (456 packages audited, 0 vulnerabilities; one upstream ESLint deprecation warning). **Backend tests:** `cd apps/api; .venv311/Scripts/python.exe -m pytest tests/test_decision_trace.py -q` => **40 passed** in 0.41s. `cd apps/api; .venv311/Scripts/python.exe -m pytest tests -q` => **1355 passed, 4 failed, 13 skipped, 2 warnings** in 161.25s. The four failures are `test_sql_migration_file_static_verification`, `test_advisor_cannot_self_grant_assignment_via_rls`, `test_student_cannot_grant_assignment_via_rls`, and `test_audit_pure_engine_has_no_infrastructure_imports`; each resolves a repository-root-relative path while the command executes from `apps/api`. Diagnostic command from repository root, `apps/api/.venv311/Scripts/python.exe -m pytest apps/api/tests -q`, => **1359 passed, 13 skipped, 2 warnings** in 83.36s. Treat the first command's four failures as a test-CWD defect, not a security acceptance result. **Frontend checks:** `npm test` => **6 files / 47 tests passed**; `npm run lint` => passed; first `npm run typecheck` => failed before Next generated its type artifacts (`src/app/layout.tsx: LayoutProps` missing); `npm run build` => passed and generated routes `/`, `/_not-found`, `/login`, `/student`; a post-build `npm run typecheck` => passed. **Supabase/security:** Supabase CLI and Docker are not installed. Existing integration tests require local-only `MORSHIDI_LOCAL_SUPABASE_URL`, `MORSHIDI_LOCAL_SUPABASE_SERVER_KEY`, and in applicable suites `MORSHIDI_LOCAL_SUPABASE_ANON_KEY`. No values were read, no production resource was touched, and no RLS/RPC claim is accepted. **P8 verification:** top P8 policy is 9,236 bytes; six companion files remain 0 bytes; Slice 1 package files are nonempty and its 40 focused tests pass; Slice 2 package/tests/docs/migration remain absent. **Security check:** no production database migration or credentials used. **Acceptance:** environment baseline is VERIFIED with the noted test-CWD defect and missing local-Supabase prerequisites; P8 Slice 2 remains NOT ACCEPTED. **Exact next starting point:** obtain authoritative source material or explicit owner-approved policy authoring for the six empty P8 companion documents, install Supabase CLI plus Docker and configure isolated local-only integration variables, then request approval before beginning any Slice 2 implementation. |
| 2026-09-24 | P8.1 policy recovery — checkpoint | **Objective:** reconstruct review-only P8.1 contracts without altering the six empty canonical policy paths or Slice 2. **Starting SHA/branch:** `721b317fbb73e6de0cadc8201c6ea1f6849433dc` / `chore/p8-recovery-baseline`. **Files changed so far:** `.gitignore` gains only `apps/api/.venv311/`; the environment is now ignored and remains on disk. **Verification:** root command `apps/api/.venv311/Scripts/python.exe -m pytest apps/api/tests -q -rs` => **1359 passed, 13 skipped, 2 warnings** in 83.02s. The exact 13 skips are all Local Supabase suites: three catalog-repository cases; one each degree-path, eligibility API, mock-registration persistence, mock-registration service, recommendation, semester-planner, student API, student repository; and two progress cases. Each reports missing local-only Supabase variables; no unrelated skip was reported. Supabase CLI and Docker remain absent, so RLS/RPC is NOT VERIFIED. **Acceptance:** P8.1 remains PARTIAL / AWAITING HUMAN APPROVAL; Slice 2 remains NOT ACCEPTED. **Next:** create six explicitly unapproved draft contracts, a draft 54-scenario candidate matrix, and a source traceability map. |
| 2026-09-24 | P8.1 policy recovery — draft completion | **Objective:** produce review-only reconstruction drafts because the six canonical companion documents are zero-byte and unrecoverable from available Git history. **Starting SHA/branch:** `721b317fbb73e6de0cadc8201c6ea1f6849433dc` / `chore/p8-recovery-baseline`. **Files changed:** `.gitignore` (narrow `apps/api/.venv311/` rule); `docs/p8-recovery/decision-trace-ledger-policy.draft.md` (6,527 bytes); `institutional-policy-retrieval-policy.draft.md` (3,847); `academic-explainability-graph-policy.draft.md` (3,550); `change-impact-policy.draft.md` (3,353); `institutional-ai-query-policy.draft.md` (3,356); `p8-grounded-knowledge-test-matrix.draft.md` (10,148); `reconstruction-source-map.md` (4,939); and this persistent log. Every draft begins `STATUS: RECONSTRUCTED DRAFT — NOT APPROVED`. **Implementation details:** the drafts preserve Slice 1 canonical hash/validation/replay/redaction behavior; derive P7 advisor conjunction and P6 grants/RLS/suppression only as security precedents; mark persistence schema, source onboarding, RAG, graph, change-impact, and query details as OPEN CONTRACT / REQUIRES HUMAN APPROVAL. The draft matrix contains 54 candidate rows (TRACE 15, RAG 15, IMPACT 8, GRAPH 8, QUERY 8); existing executable evidence is limited to Slice 1 trace tests and pre-existing P6/P7 tests. **Security:** no canonical empty file was overwritten; no application code, migration, test, production resource, or credential was modified/used. `git diff --check` passed. **Test baseline:** root backend `-q -rs` result remains **1359 passed, 13 skipped, 2 warnings**; all skips are local-Supabase prerequisites, not P8 security acceptance. **Acceptance:** P8.1 is PARTIAL / AWAITING HUMAN APPROVAL; none of these drafts is approved; P8 Slice 2 remains NOT ACCEPTED. **Exact next starting point:** human review of each recovery draft, resolution/approval of every OPEN CONTRACT, then promotion into canonical paths only by explicit approval; after separate approval and local Supabase prerequisites, plan (but do not yet implement) the smallest Slice 2 persistence/security boundary. |
| 2026-09-24 | P8.1 policy recovery — skipped-test detail | **Exact Local Supabase skips:** `test_local_catalog_maps_required_target_states`, `test_local_referenced_only_and_unknown_targets_are_distinct`, `test_local_repository_catalog_drives_engine_without_translation`, `test_real_plan12_degree_paths_scenarios_and_isolation`, `test_local_can_take_endpoint_against_accepted_catalog`, `test_p6_4_local_runtime_persistence_and_security_contract`, `test_p6_5_real_authenticated_service_api_and_aggregate_pipeline`, `test_real_plan12_structure_and_progress_scenarios`, `test_real_student_repository_catalog_service_and_engine`, `test_real_plan12_recommendations_scenarios_and_isolation`, `test_real_plan12_semester_planner_scenarios_and_isolation`, `test_local_authenticated_student_api_end_to_end`, and `test_local_student_repository_and_phase5_stack`. **Reason:** each module's `pytestmark = skipif(...)` requires local-only `MORSHIDI_LOCAL_SUPABASE_URL` and server key, with anonymous key required by applicable authenticated/API suites; progress and student-repository suites additionally require configured Plan context. **Classification:** all 13 depend on Local Supabase. No unrelated skip is present. No test condition was changed and no production credential was used. |
| 2026-09-24 | Prompt 05 — local Supabase preparation, environment checkpoint | **Objective:** prepare and verify an isolated local Supabase baseline only; no P8 Slice 2 implementation, migration, policy approval, or application behavior change. **Starting branch/SHA:** `chore/p8-recovery-baseline` / `721b317fbb73e6de0cadc8201c6ea1f6849433dc`. **Machine:** Windows 11 Pro 64-bit, version `10.0.26200` (build 26200); Node `v24.12.0`, npm `11.6.2`, and `apps/api/.venv311` Python `3.11.9` are available. **Blocking prerequisites verified:** `docker` and `supabase` commands are absent; no Docker Desktop executable was found in standard system or per-user paths. WSL is installed (`2.6.3.0`) but reports that WSL 2 is unsupported under the current machine configuration and requires the Windows Virtual Machine Platform optional feature plus BIOS virtualization. No administrator-level change, installation, restart, WSL change, container start, hosted-project connection, or production credential use occurred. Thus the isolated Supabase stack is **BLOCKED / NOT STARTED**. **Project configuration:** `supabase/config.toml` is present (15,862 bytes), specifies local API `54321`, DB `54322`, Postgres major version `17`, enabled migrations, and an enabled `./seed.sql` path; `supabase/seed.sql` is absent. The latter is a startup/reset preflight concern that must be confirmed against an actual local CLI run. Eight existing migrations were inspected in lexical order, from `0001_academic_catalog.sql` through `20260923150000_add_advisor_authorization_persistence.sql`; none was modified or applied. Migration initialization is **NOT VERIFIED**, because there is no local container runtime/CLI. **Integration variables:** current local tests require `MORSHIDI_LOCAL_SUPABASE_URL`, `MORSHIDI_LOCAL_SUPABASE_SERVER_KEY`, `MORSHIDI_LOCAL_SUPABASE_ANON_KEY` where authenticated/API paths require it, and several suites additionally require `MORSHIDI_LOCAL_STUDY_PLAN_ID`. No values were configured, read, stored, or exposed. **Current backend baseline:** `apps/api/.venv311/Scripts/python.exe -m pytest apps/api/tests -q -rs` from repository root => **1359 passed, 13 skipped, 2 warnings in 81.10s**. The exact skipped tests are the 13 named in the preceding entry, all local-Supabase suites skipped by their explicit missing-local-environment guards; none executed against PostgreSQL/Supabase and no unrelated skip appeared. The two warnings are FastAPI/Starlette test-client deprecations, not failures. **Security verification distinction:** static review confirms P6 contains RLS enablement, revokes, a `SECURITY DEFINER` persistence function, and explicit execute grants; P7 contains the advisor-student assignment table with RLS/revokes. The skipped P6.4 local test is intended to exercise student ownership, cross-student read isolation, direct insert/update/delete denial, membership mutation denial, cross-university validation, and analyst raw-data denial; P6.5 is intended to exercise authenticated service/API behavior and aggregate suppression. Neither ran. No P7 advisor assignment/revocation behavior, RLS, RPC boundary, or unauthorized write path was verified on a real database. These earlier-phase tests cannot prove future P8 ledger security. **P8 status:** P8.1 remains **PARTIAL / AWAITING HUMAN APPROVAL**; P8 Slice 2 remains **NOT ACCEPTED**. **Files changed:** only this progress record in Prompt 05; no application code, existing test, migration, configuration, or production resource changed. **Exact next starting point:** a human with system authority must enable the required Windows virtualization/Virtual Machine Platform condition (possibly requiring restart), install and start a Docker-compatible runtime, then install a project-scoped/pinned Supabase CLI; after that, start this repository's local stack, inspect generated local-only connection settings without recording values, resolve or confirm the missing configured seed path, run migration initialization, set process-only local test variables, and rerun the 13 integration tests before any Slice 2 work is authorized. |
| 2026-09-24 | Prompt 06 — Local Supabase recovery, execution checkpoint | **Objective:** establish and verify the isolated Local Supabase baseline, then execute the previously skipped database integrations; no P8 Slice 2 code, schema, or policy approval. **Starting branch/SHA:** `chore/p8-recovery-baseline` / `721b317fbb73e6de0cadc8201c6ea1f6849433dc`. **Docker:** verified operational with Docker Desktop Engine on the `desktop-linux` context; `docker version`, `docker info`, and `docker ps` completed. **CLI:** no global CLI was installed. Used temporary project-independent `npx --yes supabase@2.117.0`; it does not modify project manifests, and version `2.117.0` executed successfully. **Local startup:** `supabase start` succeeded against this repository's existing `supabase/config.toml`; no `supabase init`, link, hosted command, reset, or production access occurred. The first command wrapper mistakenly treated native Docker progress written to stderr as a terminating PowerShell error; rerunning unchanged `supabase start` with non-terminating stderr handling succeeded. **Migrations/data:** local API `54321`, PostgreSQL `54322`, Auth health, and Postgres readiness passed. Exactly eight migration-history rows exist, matching the eight local migration files through `20260923150000_add_advisor_authorization_persistence.sql`; Plan 12 exists with 68 plan-course rows. `supabase/seed.sql` remains absent although `db.seed.sql_paths` references it; startup/migration initialization nonetheless succeeded because the academic catalog/Plan 12 data is supplied by the existing idempotent migrations. Do not silently remove the seed configuration; its reset-specific behavior remains an explicit preflight concern. **Service note:** API/Auth/Postgres and required test services are healthy. `supabase_vector_Morshidi` is in a restart loop because its Docker-log source receives connection refused; this is a local stack observability issue, not an API/Auth/Postgres/migration failure, and was not changed. **Test environment:** local-only URL, server key, anon key, Plan 12 ID, plus the API's existing `SUPABASE_URL` and `SUPABASE_SECRET_KEY` were set in the pytest process only and never printed, committed, or recorded. **First full attempt:** 1366 passed, 5 failed, 1 skipped, 2 warnings in 78.80s. All five failures returned `401 AUTH_REQUIRED`, and the one skip was the eligibility API test; root cause was missing application-level `SUPABASE_URL`/`SUPABASE_SECRET_KEY`, while only `MORSHIDI_LOCAL_*` had initially been supplied. This is an environment configuration finding, not an RLS/migration/security failure. **Corrected targeted verification:** with both existing application variables supplied process-only, the eligibility, degree-path, mock-registration service, recommendation, semester-planner, and authenticated student API local tests ran and passed: **6 passed, 0 failed, 0 skipped, 2 warnings in 641.41s**. These tests are computationally heavy against real Plan 12 data. **Security evidence now executed:** P6.4 directly exercises a local database with ownership isolation, direct insert/update/delete denial, membership mutation denial, cross-university validation, and analyst raw-data denial; P6.5 exercises real Auth → FastAPI → service persistence plus analyst aggregate suppression. P8 persistence remains absent and NOT VERIFIED. **Checkpoint acceptance:** local test environment is operational; complete regression rerun remains pending. **Files changed at this checkpoint:** only this progress record; no application code, migration, test, or configuration file changed. **Exact next starting point:** run `apps/api/.venv311/Scripts/python.exe -m pytest apps/api/tests -q -rs` from repository root with the same process-only local variables, then record exact counts and unresolved Vector/seed findings. |
| 2026-09-25 | Prompt 06 — Local Supabase recovery, final baseline | **Final regression:** from the repository root, with only process-local isolated Supabase configuration, `apps/api/.venv311/Scripts/python.exe -m pytest apps/api/tests -q -rs` completed **1372 passed, 0 failed, 0 skipped, 2 warnings in 515.90s (8:35)**. This is a net execution of all 13 tests formerly skipped by Local Supabase prerequisites: the three catalog tests; degree-path; eligibility API; P6.4 persistence/security; P6.5 authenticated service/API/aggregate; two progress tests; recommendation; semester planner; authenticated student API; and student repository. No skip marker was changed. Warnings are the existing FastAPI/Starlette `httpx`/`BlockingPortal` deprecations. **Security classification:** student ownership, cross-student isolation, cross-university validation, direct client insert/update/delete denial, membership self-mutation denial, and analyst raw-record denial are **VERIFIED BY EXECUTED DATABASE INTEGRATION TEST** via P6.4. Real Auth → FastAPI → persistence behavior and institutional aggregate suppression are **VERIFIED BY EXECUTED DATABASE INTEGRATION TEST** via P6.5. The P6 migration's RLS enablement, owner-only select policies, revokes, and service-role-only `persist_mock_registration_revision` EXECUTE grant are also **STATICALLY INSPECTED**, with the execution behavior partly exercised by P6.4/P6.5. Advisor role/assignment/same-university/revocation behavior is **VERIFIED BY UNIT TEST ONLY** (`test_advisor_authorization.py` uses an in-memory repository); no advisor-specific Local Supabase integration file exists, so database enforcement of assignment revocation and P7 permissions remains **NOT VERIFIED**. Institutional individual-record denial is verified for the P6 analyst case only; it is not evidence for future P8 traces. **Remaining local findings:** `supabase_vector_Morshidi` continues to restart because its Docker log source cannot connect; API/Auth/Postgres, migrations, and required integrations still passed. Missing configured `supabase/seed.sql` did not block fresh startup because existing Plan 12 seed migrations populated data; a future isolated `db reset` must explicitly verify this configuration before it is used. **P8 status unchanged:** P8.1 remains **PARTIAL / AWAITING HUMAN APPROVAL**; Slice 1 domain core remains covered by the passing backend suite; P8 Runtime Slice 2 is **NOT IMPLEMENTED / NOT ACCEPTED**, and its ledger schema, RLS, RPC, evidence access, integrity, and replay security remain **NOT VERIFIED**. **Files changed:** this progress record only; no application code, tests, migrations, configuration, dependency manifest/lockfile, or production resource changed. **Exact next starting point:** resolve the Vector restart-loop and seed-reset preflight as separate local-infrastructure work if required; obtain human approval for the P8.1 reconstructed drafts and Category A decisions before authorizing any separate Slice 2 implementation. |
